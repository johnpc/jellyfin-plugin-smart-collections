name: Build and Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Get next version
      id: get_version
      run: |
        # Get latest tag, default to 0.0.0.0 if none
        LATEST=$(git tag --sort=-v:refname | head -1)
        if [ -z "$LATEST" ]; then
          LATEST="0.0.0.0"
        fi
        # Bump the last segment
        NEXT=$(echo "$LATEST" | awk -F. '{$NF=$NF+1; print}' OFS=.)
        echo "VERSION=$NEXT" >> $GITHUB_OUTPUT
        echo "Next version: $NEXT"

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x

    - name: Restore dependencies
      run: dotnet restore Jellyfin.Plugin.SmartCollections.sln

    - name: Build
      run: |
        VERSION=${{ steps.get_version.outputs.VERSION }}
        dotnet build Jellyfin.Plugin.SmartCollections.sln \
          --configuration Release --no-restore \
          /p:TreatWarningsAsErrors=false \
          /p:Version=$VERSION \
          /p:AssemblyVersion=$VERSION \
          /p:FileVersion=$VERSION

    - name: Create release zip
      run: |
        VERSION=${{ steps.get_version.outputs.VERSION }}
        mkdir -p release
        find . -path "*/bin/Release/net9.0/*.dll" -exec cp {} release/ \;
        cd release
        zip -r ../jellyfin-plugin-smart-collections-${VERSION}.zip .

    - name: Calculate checksum
      id: calculate_checksum
      run: |
        VERSION=${{ steps.get_version.outputs.VERSION }}
        CHECKSUM=$(md5sum jellyfin-plugin-smart-collections-${VERSION}.zip | awk '{ print $1 }')
        echo "CHECKSUM=$CHECKSUM" >> $GITHUB_OUTPUT

    - name: Generate manifest.json
      run: |
        VERSION=${{ steps.get_version.outputs.VERSION }}
        CHECKSUM=${{ steps.calculate_checksum.outputs.CHECKSUM }}
        TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
        cat > manifest.json << EOF
        [
          {
            "guid": "09612e52-0f93-41ab-a6ab-5a19479f5315",
            "name": "Smart Collections",
            "overview": "Enables creation of Smart Collections based on Tag",
            "description": "Enables creation of Smart Collections based on Tag",
            "owner": "johnpc",
            "category": "General",
            "versions": [
              {
                "version": "${VERSION}",
                "changelog": "See the full changelog at [GitHub](https://github.com/johnpc/jellyfin-plugin-smart-collections/releases/tag/${VERSION})",
                "targetAbi": "10.11.0.0",
                "sourceUrl": "https://github.com/johnpc/jellyfin-plugin-smart-collections/releases/download/${VERSION}/jellyfin-plugin-smart-collections-${VERSION}.zip",
                "checksum": "${CHECKSUM}",
                "timestamp": "${TIMESTAMP}"
              }
            ]
          }
        ]
        EOF

    - name: Create tag and GitHub Release
      run: |
        VERSION=${{ steps.get_version.outputs.VERSION }}
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git tag "$VERSION"
        git push origin "$VERSION"
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Upload Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ steps.get_version.outputs.VERSION }}
        files: |
          jellyfin-plugin-smart-collections-${{ steps.get_version.outputs.VERSION }}.zip
          manifest.json
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Update manifest on main
      run: |
        git fetch origin main
        git checkout main
        git add manifest.json
        git commit -m "chore: update manifest for ${{ steps.get_version.outputs.VERSION }}" || true
        git push origin main
